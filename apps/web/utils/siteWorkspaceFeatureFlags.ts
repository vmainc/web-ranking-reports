/**
 * When `false`, Cloudflare is hidden from **site** workspace UI (site home “Site data” / add menu,
 * site settings “What’s connected”). Dashboard → Integrations → Cloudflare, APIs, cron jobs, and
 * report builder modules stay available. Set to `true` to show Cloudflare on sites again.
 */
export const SHOW_CLOUDFLARE_IN_SITE_WORKSPACE = false
