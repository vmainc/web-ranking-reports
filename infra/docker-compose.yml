# Web Ranking Reports — MVP stack
# Run from repo root: docker compose -f infra/docker-compose.yml up -d

services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - WEBRANKINGREPORTS_DOMAIN=${WEBRANKINGREPORTS_DOMAIN:-webrankingreports.com}
      - POCKETBASE_SUBDOMAIN=${POCKETBASE_SUBDOMAIN:-pb}
    depends_on:
      - web
      - pb

  web:
    build:
      context: ..
      dockerfile: infra/Dockerfile.web
      args:
        - NUXT_PUBLIC_POCKETBASE_URL=${NUXT_PUBLIC_POCKETBASE_URL:-https://pb.webrankingreports.com}
        - APP_URL=${APP_URL:-https://webrankingreports.com}
    image: web-ranking-reports-web
    restart: unless-stopped
    env_file:
      - .env
    # Do not override env_file with empty values: PB_* and secrets come only from infra/.env
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - NODE_ENV=production
    expose:
      - "3000"

  pb:
    build:
      context: .
      dockerfile: Dockerfile.pb
    restart: unless-stopped
    volumes:
      - pb_data:/pb_data
    environment:
      - POCKETBASE_DEBUG=0
    expose:
      - "8090"

volumes:
  caddy_data:
  caddy_config:
  pb_data:
